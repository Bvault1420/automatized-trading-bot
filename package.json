{
  "name": "aegis-trading-bot",
  "private": true,
  "version": "1.0.0",
  "description": "Paper-First Trading-Bot mit Qualitätsfilter, SL/TP-Stufen, Selbsttest und 24/7-Betrieb",
  "workspaces": ["server", "web"],
  "scripts": {
    "dev": "concurrently -k -n SERVER,WEB -c cyan,magenta \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w server && npm run build -w web",
    "start": "npm run start -w server",
    "start:always": "bash scripts/always-on.sh",
    "localhost": "bash scripts/localhost.sh",
    "public": "bash scripts/keep-public.sh",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w web",
    "test": "npm run test -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
